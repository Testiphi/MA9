# 两车换车测试

任务：`两车换车测试（J50缺油换Panamera，不开赛）`。

最新滑动时序统一由 `tools/vehicle_search.py` 的 `swipe_timing()` 提供：400 像素、320ms、动作前延迟 0、滑后等待 350ms。下方较早实测中的 400ms/600ms 是此前速度；本次只压缩时间，不扩大步长或改识别门槛。

暂用 J50 → Porsche Panamera Turbo 的测试顺序，不改 Excel 或生成的正式轮换数据。独立复制导航节点，避免第二阶段重复定位黄金时受第一阶段的 max_hit 限制。

J50 可开始时确保 TouchDrive 开启并结束；J50 缺油时点击左上返回，确认车辆列表，再自动处理仅拥有开关、点击黄金回起点、搜索 Panamera。搜索每次先检查车型标识，再从 (1100,420) 滑至 (650,420)，400ms，最多 12 次。选中 Panamera 后确认车型、开始按钮与 TouchDrive 开启状态，然后结束。不会点击车辆详情页的比赛开始、跳过或补油按钮。

刷新 Maa Pipeline Support 后，从 J50 缺油详情页运行，检查是否返回列表、定位黄金、找到 Panamera，并停在其详情页。成功终点：`两车_Panamera_TouchDrive_已开启`。也支持原 J50 搜索任务的主页、介绍页和列表起点；从可开始的 Panamera 详情页运行会直接确认开关并结束。

Panamera 缺油时，车型、0/ 燃油前缀、跳过文字和返回箭头同时命中后点击左上返回；只确认列表标题和段位导航条后结束，不要求仅拥有开启或黄金选中。终点：`两车_Panamera缺油_已返回_测试结束`。从 Panamera 缺油详情直接运行可单独验证此分支；从 J50 缺油开始且 Panamera 也缺油时，验证两辆测试车均跳过后的结束流程。该终点只表示此两车测试结束，不代表整份正式名单已耗尽。

Panamera 完整车名为 Porsche Panamera Turbo S，截图文件名包含末尾 S。详情身份模板使用左侧不滚动的小卡片车辆图和印刷车型名，不使用滚动的大标题。缺油数字、跳过文字、返回箭头复用 J50 模板。27 张截图的正反例检查及遮掉滚动标题后的有油、缺油识别检查通过。入口直接列出 Panamera 与 J50 的实际识别节点，避免 Panamera 首次漏识别后被 DirectHit 兜底永久切入 J50 路线。TouchDrive 关闭分支沿用已验证的共用开关模板，仍需设备验证。段位不可用、未拥有或搜索耗尽仍会超时，尚未实现继续换第三辆。

生成与检查：先运行 `tools/prepare_j50_search.py`，再运行 `tools/prepare_two_car_rotation.py`，最后运行 `tools/validate_schema.py --schema-dir deps/tools`。27 张截图用于车型列表、详情、缺油与返回完成识别的正反例检查。J50 缺油换有油 Panamera 流程用户已确认成功，新增 Panamera 缺油返回分支仍需设备实测。

## 列表漏识别定位

20:44 的日志显示列表车型模板有候选得分 0.882339，未达到 0.9，随后继续滑动。列表模板改为只截卡片印刷车型名，不包含周围背景；搜索 ROI 扩为整个两行车辆区，覆盖靠左的完整名称。列表修复与原正向路线已由用户实测通过。左右平移截图的识别检查通过。

新增独立任务 `验证Panamera列表识别（原地选车，不滑动）`。手动让 Panamera 卡片名称完整可见，黄金选中、仅拥有开启，然后运行此任务。应点击车型标识进入详情，有油时确认 TouchDrive，缺油时返回列表结束。此任务不会滑动或自动回段位起点。若 10 秒仍未识别，将保存错误截图，便于检查车辆仍可见时的真实匹配差异，而不是滑过后的画面。

## 白金起点反查

J50 和 Panamera 正向搜索均为 (1000,420) → (600,420)、400ms、滑后等待 600ms、最多 12 次，与用户已确认稳定的反向搜索速度相同。Panamera 达到上限后，点击白金图标 (726,107)，等待 1500ms；确认列表页、仅拥有开启、白金高亮和截图中的起点 GT-R NISMO 卡片标识后，再反向搜索。该起点标识来自当前账号截图，换账号或车辆列表变化时需要重新验证。

反向滑动 (600,420) → (1000,420)，400ms，每次等待 600ms，最多 24 次。此前 220 像素、500ms、等待 1000ms 的反查路线已由用户实测成功，但找到车辆需要二十多次滑动，因此改为 400 像素并缩短等待，调整后的速度需设备验证。每次先识别 Panamera 再滑动。反查识别不要求黄金高亮，因为从白金滑回黄金车辆时仍可能保留白金标签。缺油返回列表的完成判断也接受白金导航条外观。反查仍没找到时，停止在 `两车_Panamera_滑动搜索_反查未找到_测试停止`；这只表示搜索预算耗尽，不能判断该车未拥有。目前未实现滑动停滞检测或黄金/白银区间的视觉边界检测。

新增独立任务 `验证Panamera反向搜索（白金起点，不开赛）`：从选车列表且仅拥有开启时运行，会主动点白金并验证起点，再往回搜索 Panamera。找到后沿用原车型详情、TouchDrive 与缺油处理。共用构建函数位于 `tools/vehicle_search.py`，当前只接入 Panamera，J50 搜索保留原行为。28 张截图、左右平移与白金高亮下的黄金车型识别检查通过，新增反查路线仍需设备实测。
