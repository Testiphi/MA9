# MA9 进度存档：2026-09-15

运行环境为 MuMu 模拟器、ADB、1280×720 标准截图，使用 Maa Pipeline Support 调试。

## 已实现

- 中文主页标签导航、每日赛事与多人入口、返回上一级及返回主页。
- 多人共用选车流程：段位起点、仅拥有、双向滑动搜索、车辆详情识别、缺油返回及轮换。黄金轮换采用已登记素材和人工维护的顺序，停在准备界面，不自动开赛。
- TouchDrive 开启检查；车辆详情通过稳定车卡区域识别，避开长车名滚动。
- 多人成绩、奖励、错失机会、降级确认、返回系列赛页面的结算流程。
- 神山垭口／坠落加载页与多人 HUD 识别。
- 英文主页进入设置、切换简体中文、关闭重启提示并等待中文主页。重启识别兼容两种标题。
- 可选弹窗处理：推广广告右上角 ×、连接错误重试；没有 × 的车库外观广告保留立即领取并返回主页。

车辆原始 CSV、选车 Excel 和人工识别配置位于 `data/sources/`，导入结果位于 `data/generated/`。原始截图位于 `captures/`，裁剪模板位于 `assets/resource/image/navigation/`，生成脚本位于 `tools/`。

## 验证与限制

此前用户已验证导航、黄金选车、缺油返回及滑动提速。最新语言切换与通用弹窗的 814 项离线截图识别检查通过，所有 Pipeline 和界面配置通过 schema 校验；最新弹窗点击、重连和重启后的设备衔接仍需验证。

尚未实现完整自动比赛循环。赛道百分比 OCR 和按百分比执行赛道操作仍待开发；未补齐所有车辆和所有缺油状态素材。当前段位人工配置为黄金，升降段后的选车偏好需更新。多人开发暂缓。

## 继续工作

刷新 Maa Pipeline Support，优先验证“确保简体中文（英文自动切换并重载）”和“通用弹窗（广告关闭，连接重试，返回主页）”。新弹窗识别失败时保留对应标准截图，再添加回归样例。

校验命令：

```powershell
python -X utf8 tools/prepare_language.py
python -X utf8 tools/validate_schema.py --schema-dir deps/tools
```
